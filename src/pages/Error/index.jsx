import ErrorPage from "../../components/ErrorPage";

 function Error() { // S'il y a erreur sur la path, on est redirigé vers la page erreur//
	return (
		<>
			<ErrorPage />
		</>
	);
}
export default Error;